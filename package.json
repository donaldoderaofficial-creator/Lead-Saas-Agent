{
  "name": "lead-agent-saas",
  "version": "1.0.0",
  "description": "Event-driven lead-qualification agent exposed as an HTTP API",
  "main": "server.js",
  "author": "Donald Odera",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check store.js && node --check compliance.js && node --check auth.js && node --check lead-pipeline.js && node --check paypal-client.js && node --check mpesa-client.js && node --check explorium-client.js",
    "test": "npm run check && node --test",
    "commit": "bash scripts/generate-commit-message.sh --commit",
    "commit:preview": "bash scripts/generate-commit-message.sh --preview",
    "commit:analyze": "bash scripts/generate-commit-message.sh --analyze",
    "commit:interactive": "bash scripts/generate-commit-message.sh"
  },
  "dependencies": {
    "@paypal/checkout-server-sdk": "^1.0.3",
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^13.0.3",
    "compression": "^1.8.1",
    "cross-fetch": "^3.2.0",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "express-session": "^1.19.0",
    "otplib": "^12.0.1",
    "qrcode": "^1.5.4"
  }
}
